/**
 * Pulse Web — components/UserChip.tsx
 * Version: v0.1.1
 * Purpose: Placeholder header chip (kept for future use). Not rendered by default.
 */

import React from "react";

/** UserChip
 * v0.1.1 — Minimal stub to satisfy imports. Returns null.
 */
export default function UserChip() {
  return null;
}
