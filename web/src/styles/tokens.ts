/**
 * Pulse — UI Tokens
 * Version: v0.1.0
 * Shared style tokens used across panels, popovers, sheets, and controls.
 *
 * Tweak these values to change the global "glass" look app‑wide.
 */
export const GLASS = {
  // Panel container (match "Me" menu vibe)
  panel: "bg-white/15 backdrop-blur-2xl border border-white/10 ring-1 ring-white/10 shadow-lg",
  // Surfaces inside the panel (lists, inputs, pills background on hover)
  surface: "bg-white/10 border border-white/15",
  // Buttons inside panel
  button: "border border-white/15 bg-white/20 hover:bg-white/25",
};
